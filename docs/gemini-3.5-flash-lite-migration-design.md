# Gemini 3.5 Flash-Lite移行設計

## 1. 目的

BB Cafe Messagesのメッセージ生成について、主系を`gemini-3.5-flash-lite`、フォールバックを`gemini-2.5-flash`へ変更する。

対象は手動生成とCronによる自動生成の共通サーバー処理とする。プロンプト、生成結果の保存、LINE送信、画面表示の仕様は変更しない。

## 2. 現状と決定事項

### 2.1 現状

- `GEMINI_MODEL`を主系、`GEMINI_FALLBACK_MODELS`を後続候補として使用する。
- 一時的なHTTPステータス（429、500、502、503、504）の場合だけ、モデルごとに設定回数を再試行して次の候補へ進む。
- 現在のリクエストは全候補モデルに同じ`generationConfig`を送っている。
- 現在の設定は`temperature: 0.8`、`thinkingConfig.thinkingBudget: 0`。
- 自動送信Cronは`generateDailyGreetingMessage`を呼び出すため、手動生成と同じモデル切替を受ける。

### 2.2 採用する設定

```env
GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_FALLBACK_MODELS=gemini-2.5-flash
```

コード上のデフォルト値と`.env.example`も同じ順序に合わせる。デプロイ環境のEnvironment Variablesは実装検証後に更新する。

### 2.3 採用理由

- 3.5 Flash-Liteは低遅延・高スループット向けで、短い挨拶文生成の主系候補として適合する。
- 2.5 Flashは現行主系であり、既存の生成品質と互換性を持つフォールバックとして扱いやすい。
- フォールバックは通常経路ではないため、コスト最小化よりも生成継続性と品質を優先する。
- 3.5系では`temperature`などのサンプリング設定を削除し、`thinkingLevel`を使用する必要があるため、環境変数だけの変更にはしない。

## 3. リクエスト生成設計

### 3.1 モデル別プロファイル

候補モデルをループする際に、モデルごとに生成設定を構築する。プロンプトと`contents`は候補間で同一にし、モデル別に変えるのは`generationConfig`だけとする。

| モデル | `maxOutputTokens` | 思考設定 | `temperature` | 意図 |
| --- | ---: | --- | --- | --- |
| `gemini-3.5-flash-lite` | 300 | `thinkingConfig.thinkingLevel: "minimal"` | 送信しない | 短文生成の低遅延を優先しつつ、3.xの標準設定に合わせる |
| `gemini-2.5-flash` | 300 | `thinkingConfig.thinkingBudget: 0` | `0.8` | 現行フォールバックの挙動をできる限り維持する |

3.xリクエストには`temperature`、`topP`、`topK`、`thinkingBudget`を含めない。2.5系リクエストには`thinkingLevel`を含めない。

### 3.2 実装境界

既存の責務を次のように維持する。

```text
generateDailyGreetingMessage
  -> 共通プロンプトを作成
  -> モデル候補を順番に処理
  -> モデル別generationConfigでRESTリクエストを作成
  -> 一時エラーだけ再試行・次候補へ切替
  -> 既存の応答検証・重複判定・保存処理へ返す
```

`generateGeminiContent`にJSON化済みの単一`body`を渡す方式は、候補モデルごとにモデル別設定を適用できる方式へ変更する。候補ごとに同じプロンプトを再生成せず、入力内容の差異が発生しないようにする。

### 3.3 モデル候補の解決

- 候補順は`[主系, フォールバック1, ...]`を維持する。
- 重複モデルは既存どおり1回にまとめる。
- 空文字、空要素は候補に含めない。
- 対象の2モデル以外を環境変数で指定した場合は、既存のカスタム設定を壊さないため、2.5系互換設定を既定とする。ただし3.xモデル名を指定した場合は3.x設定を使う。
- 将来モデルを追加する際に設定差異を見落とさないよう、モデル判定と生成設定の対応を1か所に集約する。

## 4. フォールバック状態遷移

| 状態 | 条件 | 次の動作 | 意図 |
| --- | --- | --- | --- |
| 主系送信 | `gemini-3.5-flash-lite`へ送信 | 成功なら終了 | 通常経路を1回で完了する |
| 主系一時失敗 | 429/500/502/503/504 | 設定回数まで同じモデルを再試行 | 瞬間的な混雑を吸収する |
| 主系再試行上限 | 再試行後も一時失敗 | `gemini-2.5-flash`へ切替 | 送信機会を残す |
| フォールバック成功 | 2.5 Flashが成功 | 成功結果を既存処理へ返す | UI・Cronの後段を変えない |
| 全候補一時失敗 | 全モデルが上限まで失敗 | 既存のエラー履歴・通知処理へ返す | 無限再試行を防ぐ |
| 非一時エラー | 400等 | 既存どおり即時失敗 | 不正な設定や契約違反をフォールバックで隠さない |

モデル切替は同一生成試行内で行う。生成済みテキストを別モデルへ渡して再編集することはしない。

## 5. テスト観点

ケース作成前に、次の観点を受入れ対象とする。

### 5.1 機能観点

- 主系が3.5 Flash-Lite、フォールバックが2.5 Flashの順で呼ばれること。
- 主系成功時に不要なフォールバックを呼ばないこと。
- 一時エラー時だけ再試行・切替し、非一時エラーは即時失敗すること。
- 手動生成と自動送信Cronが同じ切替ルールを使うこと。
- 既存の天気、誕生日、記念日、直近の冒頭文制約が両モデルに同じ内容で渡ること。
- 天気の最高気温は、気象庁レスポンスの`timeDefines`と気温配列を対応付け、生成対象日のJSTの日付に一致する値だけから算出すること。
- 手動生成と自動送信Cronで、同じ生成対象日を天気取得処理にも渡すこと。

### 5.2 非機能観点

- 3.5系リクエストに非推奨の`temperature`や`thinkingBudget`が混入しないこと。
- 2.5系フォールバックに`thinkingLevel`が混入しないこと。
- 再試行回数、`Retry-After`、遅延設定が既存どおり機能すること。
- 主系成功時のリクエスト数、フォールバック時の総待ち時間、エラー率を比較できること。
- APIキー、プロンプト全文、生成本文を新たなログへ出力しないこと。

### 5.3 データ観点

- モデル切替によってプロンプトの天気・カレンダー情報が欠落、重複、改変されないこと。
- 生成結果の保存先、日付、`lineAccountId`、送信履歴の関連付けが変わらないこと。
- フォールバック成功時も成功結果として扱い、失敗履歴を誤登録しないこと。
- 全候補失敗時に既存の部分結果・エラー要約を維持すること。

### 5.4 UI観点

- 手動生成画面の成功、失敗、再生成制御が変わらないこと。
- Cron完了通知と失敗通知の文面・件数が変わらないこと。
- フォールバックした事実をユーザー本文へ混入させないこと。

### 5.5 日次注意情報

- 天気は文字列を再解釈せず、気象庁の対象日データから天気概要、天気コード、最高・最低気温、降水確率、風情報を構造化して扱う。
- 暑さ対策は環境省の公式WBGT予測（名古屋地点`51106`）を優先し、対象日と一致する時刻列だけを集計する。WBGTの25、28、31を注意情報の境界として扱い、WBGT取得失敗時も気象庁データによる生成を継続する。
- 雨・雪・風・凍結リスクを天気概要、降水確率、風情報、最低気温から判定する。雪と最低気温0度以下は凍結のおそれ、雨雪は転倒予防、強い風は外出時の注意として表現する。
- 注意情報は優先度順に最大2件までプロンプトへ追加する。感染症情報（新型コロナ・インフルエンザ等）は別の公式データ連携が必要なため今回の対象外とし、季節だけで断定的な案内はしない。
- 手動生成とCronは同じ日付コンテキストを共有し、対象日の取り違えを防ぐ。公式情報の参照元は[環境省の暑さ指数データ提供サービス](https://www.wbgt.env.go.jp/data_service2.php)と[気象庁の防災情報](https://www.jma.go.jp/jma/bosaiinfo/snow_portal.html)とする。

### 5.6 第2段階の外部公式情報

- 既存の日次送信設定に`externalCareSignalsEnabled`を追加し、管理画面のトグルで感染症週報、環境省の熱中症アラート、気象庁の愛知県警報・注意報の利用を切り替える。未保存の既存設定は`false`とし、手動生成とCronは同じアカウント設定を参照する。
- 感染症は名古屋市単独ではなく、公式データの粒度に合わせて愛知県のインフルエンザ・COVID-19を扱う。報告週、公表日時、速報値であることを構造化し、当日データとして表現しない。
- 感染症は最新週・前週・前々週がそろい、定点当たり報告数が2週連続で増加した場合だけ「増加傾向」とする。公表から14日を超えたデータ、地域欠落、数値不正時は注意情報を出さない。
- インフルエンザの従来の警報・注意報基準値を自動判定に使わない。定点医療機関の変更により公式の警報・注意報システムが停止しているため、数値だけで「流行中」と断定しない。
- 感染症注意情報は、インフルエンザとCOVID-19が同時に該当しても1件にまとめる。手洗い、体調不良時に無理をしない等の一般的な行動に限定し、診断・治療・服薬の案内はしない。
- 感染症週報の同一性は、情報元URL、報告週、公表日時、対象感染症を連結したキーで判定する。公表日時が変わった修正版や対象感染症の変化は新しい注意情報として扱う。
- 同じ週報は、前回のLINE送信から3日未満であれば抑制し、3日経過後は再表示を許可する。表示履歴はLINEアカウント単位で保存し、手動送信とCronで共有する。
- 手動作成のプレビューでは表示履歴を更新せず、手動送信またはCronで少なくとも1件のLINE送信が成功した時点で`lastDisplayedAt`を更新する。
- 環境省の熱中症警戒・特別警戒アラートは、WBGT数値より優先する。気象庁の雷、大雨、強風、大雪、風雪等の注意報は、天気文字列による推測より優先する。
- 特別警報・警報はモデルによる自由な言い換えに依存せず、公式種別を構造化する。通常の挨拶文へ追加する注意報と、将来の緊急通知で扱う警報以上を区別する。
- 外部情報は`Promise.allSettled`相当で個別に取得し、感染症・WBGTアラート・警報のいずれかの障害で天気メッセージ全体を失敗させない。注意情報は既存どおり最大2件とする。
- 外部情報の設定取得に失敗した場合は`false`として扱い、外部情報なしでメッセージ生成を継続する。環境変数による有効化フラグは持たない。

## 6. テストケース

前提として、APIキーはテスト用の値、天気・Firestore依存はモック、同一テスト内の`fetch`は逐次呼出しとする。外部APIの実呼出しやLINE送信は単体テストで行わない。

| ID | 区分 | ケース | 検証意図 |
| --- | --- | --- | --- |
| G-001 | 正常系 | 主系3.5 Flash-Liteが成功 | 新しい主系が正しいURLと3.x設定で1回だけ呼ばれることを確認する |
| G-002 | 正常系 | 主系成功後の呼出し数 | 成功時に2.5 Flashへ不要なフォールバックをしないことを確認する |
| G-003 | 正常系 | 2.5 Flashフォールバック成功 | 3.5系の一時失敗後、2.5系設定で成功結果を返すことを確認する |
| G-004 | 異常系 | 429/500/502/503/504 | 各一時ステータスが再試行・候補切替の対象であることを確認する |
| G-005 | 異常系 | 400/401/403 | 非一時エラーがフォールバックで隠されず、既存の表示要約になることを確認する |
| G-006 | 異常系 | 全候補が一時失敗 | 最終ステータスとエラー要約を保持して終了することを確認する |
| G-007 | 境界値 | 再試行回数0、1、異常値 | 余分な呼出しや無限再試行が発生しないことを確認する |
| G-008 | 境界値 | 空のフォールバック設定、重複候補 | 主系だけ、または重複除去後の順序で動作することを確認する |
| G-009 | リクエスト | 3.5系設定 | `thinkingLevel: "minimal"`のみを送り、`temperature`と`thinkingBudget`を送らないことを確認する |
| G-010 | リクエスト | 2.5系設定 | `temperature: 0.8`と`thinkingBudget: 0`を送り、`thinkingLevel`を送らないことを確認する |
| G-011 | データ | 主系失敗後の再送 | 天気・予定・直近冒頭文のプロンプトが候補間で同一であることを確認する |
| G-012 | 状態遷移 | 主系再試行→フォールバック成功 | 途中失敗から最終成功へ遷移し、失敗扱いを残さないことを確認する |
| G-013 | 状態遷移 | 全候補失敗 | 成功保存・LINE送信へ進まず、既存の失敗処理へ遷移することを確認する |
| G-014 | 回帰 | 既存の生成本文検証 | 空本文、`MAX_TOKENS`、禁止された終了理由、冒頭重複の既存挙動を維持することを確認する |
| W-001 | 正常系・データ | 今日より明日の気温が高い | 今日の`timeDefines`に対応する値だけから最高気温を算出し、明日の値を採用しないことを確認する |
| W-002 | 異常系・データ | 今日の短期予報が欠落 | 週間予報の今日の値が有効な場合だけ今日の値を採用することを確認する |
| W-003 | 境界値 | 今日の値が空欄、明日の値が有効 | 明日の値を誤採用せず、気温フォールバックへ遷移することを確認する |
| W-004 | 境界値 | JSTの日付境界 | UTC上では前日でもJSTで当日となる生成日時を、当日の予報日として扱うことを確認する |
| C-001 | 正常系・データ | WBGT 25、28、31の境界 | 公式WBGTの対象日データを摂氏へ変換し、注意情報の境界を正しく適用することを確認する |
| C-002 | 異常系・状態遷移 | WBGTのタイムアウト、503 | WBGTだけが取得できない場合も、気象庁の天気情報で生成を継続することを確認する |
| C-003 | 正常系・機能 | 雨・雪・降水確率・風情報 | 当日の天候に応じた転倒予防、強風注意を追加し、月だけでは判定しないことを確認する |
| C-004 | 境界値・データ | 雪かつ最低気温0度、雨のみ、乾燥した寒日 | 凍結注意の条件を過剰適用せず、雨雪注意と区別することを確認する |
| C-005 | 状態遷移・UI | 熱中症、雨雪、風が同時成立 | 優先度の高い注意を最大2件に絞り、生成プロンプト末尾の注意情報として安定して渡すことを確認する |
| E-001 | 正常系・データ | 愛知県の最新週報、3週分の数値 | Shift-JIS CSVから報告週、作成日、愛知県の定点当たり報告数を正しく抽出することを確認する |
| E-002 | 正常系・機能 | インフルエンザ/COVID-19が2週連続増加 | 感染症名を1件の一般的な感染症対策へまとめ、「増加傾向」としてだけ案内することを確認する |
| E-003 | 境界値 | 公表から14日、15日 | 14日以内は利用し、15日以上はstaleとして注意情報を出さないことを確認する |
| E-004 | 異常系・状態遷移 | 感染症CSV欠落、文字化け、愛知県行欠落 | 感染症情報だけを無効化し、天気情報の生成を継続することを確認する |
| E-005 | 正常系・機能 | 雷・大雨・強風・大雪の気象庁注意報 | 公式注意報を天気文字列の推測より優先し、同種の注意を重複表示しないことを確認する |
| E-006 | 優先度・境界値 | WBGT警戒/特別警戒アラート | WBGT数値より公式アラートを優先し、熱中症注意を1件に統合することを確認する |
| E-007 | 異常系・状態遷移 | 外部3ソースの一部または全部が失敗 | 取得失敗を本文へ混入させず、利用可能な天気情報だけで生成を完了することを確認する |
| E-008 | 正常系・状態遷移 | 管理画面で外部情報をOFF→ON→OFF | 同じアカウントの手動生成とCronが設定状態に応じて外部情報の取得を切り替えることを確認する |
| E-009 | 境界値・データ | 既存の設定ドキュメントに項目がない | 旧データを読み込んだ場合に外部情報を誤って有効化しないことを確認する |
| E-010 | 異常系・状態遷移 | 手動生成時の設定取得失敗 | 設定をOFF扱いにして、外部情報なしの生成を継続することを確認する |
| E-011 | 正常系・データ | 情報元URL・報告週・公表日時・対象感染症 | 同じ週報のキーが同じ値になり、対象感染症や公表日時の変化が別キーになることを確認する |
| E-012 | 境界値・状態遷移 | 同じ週報を送信後0〜2日、3日 | 3日未満は抑制し、3日経過時は再表示することを確認する |
| E-013 | 正常系・状態遷移 | 手動プレビュー、手動送信、Cron送信 | プレビューでは履歴を更新せず、送信成功時だけ手動・Cron共通の履歴を更新することを確認する |

## 7. 実行範囲と証跡

### 実装時に必須

- `src/features/messages/server/gemini.test.ts`、`src/features/messages/server/weather.test.ts`、`src/features/messages/server/dailyCare.test.ts`、`src/features/messages/server/externalCareSignals.test.ts`、`src/app/api/message-assistant/send/route.test.ts`の対象テストを更新・追加する。
- `npm run test -- src/features/messages/server/gemini.test.ts src/features/messages/server/weather.test.ts src/features/messages/server/dailyCare.test.ts src/features/messages/server/externalCareSignals.test.ts src/app/api/message-assistant/send/route.test.ts`相当の対象単体テストを実行する。
- `npm run typecheck`、`npm run lint`、`npm run build`を実行する。
- `fetch`の呼出し順、URL、リクエストボディのモデル別設定をテスト失敗時の証跡として残す。

### 本番切替前に必須

- 同一入力を両モデルへ与え、少なくとも通常時、予定あり、悪天候、直近文ありのケースを比較する。
- 日本語の自然さ、冒頭の時刻挨拶、事実誤り、文字数、重複、生成時間を評価する。
- Cronの実行確認はLINE送信を伴うため、対象ユーザー・未送信状態・実行日時を明示してから単独で行う。
- 実機ブラウザーE2Eの全件実施は不要。今回の変更はサーバー側生成モデルとCron経路が中心のため、対象単体テストと限定的なCron確認を優先する。

### 未実施範囲

- 本変更ではGeminiモデル設定、既存の保存・送信フロー、本番環境の環境変数は変更しない。
- 実際のGemini API品質、料金、レイテンシーはAPIキー・本番相当データ・実行枠が必要なため、設計書作成時点では未検証とする。
- UIの見た目確認は対象外。ただし既存エラー表示とCron通知の回帰確認は実装後に行う。

## 8. ロールバック

品質低下、400エラー、レイテンシー悪化、フォールバック率増加が確認された場合は、環境変数を次へ戻す。

```env
GEMINI_MODEL=gemini-2.5-flash
GEMINI_FALLBACK_MODELS=gemini-2.5-flash-lite
```

ロールバックはプロンプト・保存データ・送信履歴を変更せず、モデル候補の設定だけを戻す。実装後は、ロールバック設定でも2.5系の既存テストが通ることを受入条件とする。

## 9. 参照

- [Google AI for Developers: Gemini 3.5 Flash-Lite](https://ai.google.dev/gemini-api/docs/models/gemini-3.5-flash-lite)
- [Google AI for Developers: Using the latest Gemini models](https://ai.google.dev/gemini-api/docs/latest-model)
- [Google AI for Developers: Gemini Generate Content API thinking](https://ai.google.dev/gemini-api/docs/generate-content/thinking)
